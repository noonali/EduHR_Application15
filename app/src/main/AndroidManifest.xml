<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hanan.eduhr_application">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChooseUserType" />
        <activity android:name=".LoginAdmin" />
        <activity android:name=".SigninCurrentTeacher" />
        <activity android:name=".SignupNewTeacher" />
        <activity android:name=".CurrentTeacherMainPage" />
        <activity android:name=".NewTeacherMainPage" />
        <activity android:name=".SearchTeacherByID" />
        <activity android:name=".PersonalityQuestions" />
        <activity android:name=".ShowSearchResults" />
        <activity android:name=".EnterPersonalInfo" />
        <activity android:name=".UpdatePersonal_Info"></activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
</manifest>